[phases.setup]
nixPkgs = ["nodejs_20", "libreoffice-fresh"]

[phases.install]
cmds = ["cd backend && npm install && cd ../frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "node backend/server.js"
